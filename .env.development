# Local development serves the landing page and the app on a single host
# (http://localhost:3000), so the production domain split is intentionally OFF:
# with these empty, middleware.ts performs no host-based redirects locally.
NEXT_PUBLIC_APP_URL=
NEXT_PUBLIC_MARKETING_URL=
